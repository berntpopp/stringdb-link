# Production docker-compose override
# Usage: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  stringdb-link:
    # Production-specific environment variables
    environment:
      LOGGING__LEVEL: INFO
      LOGGING__FORMAT: json
      RELOAD: "false"
      ALLOWED_HOSTS: '["localhost","127.0.0.1","::1","stringdb-link.genefoundry.org"]'
    
    # Remove development volume mounts
    volumes: []
    # Never publish a host port in prod: expose-only behind the reverse proxy.
    # `!reset` (not []) is required because Compose MERGES list fields across -f
    # overlays — a plain `ports: []` would leave the base ${PORT:-8000}:8000 map.
    ports: !reset []

    # Container & Deployment Hardening Standard v1 (§2-4): read-only rootfs with
    # an explicit tmpfs scratch, all capabilities dropped, no privilege
    # escalation, a real init for signal/zombie handling, and a PID cap.
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    init: true
    pids_limit: 256

    # Run REST plus hosted MCP through the project CLI.
    command: ["stringdb-link", "server", "--transport", "unified", "--host", "0.0.0.0", "--port", "8000"]

    # Production resource limits
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
          pids: 256
        reservations:
          memory: 512M
          cpus: '0.5'
      
      # Production restart policy
      restart_policy:
        condition: any
        delay: 5s
        max_attempts: 3
        window: 120s
    
    # Enhanced health check for production
    healthcheck:
      test: ["CMD", "curl", "-f", "-H", "Host: localhost", "http://localhost:8000/api/health"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 30s
    
    # Production logging with rotation
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"
        labels: "service=stringdb-link,environment=production"
